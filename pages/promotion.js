import React from 'react';
import MainLayout from '@/layouts/MainLayout';

const Promotion = () => {
    return (
        <div>
            <h1>Promotion</h1>
        </div>
    );
};

Promotion.Layout = MainLayout;

export default Promotion;
